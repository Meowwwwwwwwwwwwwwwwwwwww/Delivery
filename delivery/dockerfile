# 1. Use an official Python base image
FROM python:3.11-slim

# 2. Set the working directory inside the container
WORKDIR /usr/src/app

# 3. Set the environment variable for Gunicorn/Django to bind to the port
ENV PORT 8000
ENV PYTHONUNBUFFERED 1

# 4. Copy and install dependencies
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

# 5. Copy the rest of the application code
COPY . .

# 6. Run collectstatic and migrations during the build to ensure they only happen once
RUN python manage.py collectstatic --noinput
RUN python manage.py migrate --noinput

# 7. Expose the port (Gunicorn will listen on this port)
EXPOSE 8000

# 8. Define the command to run when the container starts
# REPLACE 'config' with your actual Django project folder name!
CMD ["gunicorn", "config.wsgi:application", "--bind", "0.0.0.0:8000"]